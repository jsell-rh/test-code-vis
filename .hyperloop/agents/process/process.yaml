apiVersion: hyperloop.io/v1
kind: Process
metadata:
  name: default
repo: jsell-rh/test-code-vis
pipeline:
  - loop:
      - agent: implementer
      - agent: verifier
      - agent: spec-reviewer
  - action: merge-pr

hooks:
  after_reap:
    - type: process-improver
