# Intake Record — 2026-04-25

## Specs processed

| Spec | Decision | Reason |
|---|---|---|
| `specs/core/system-purpose.spec.md` | **No tasks** | Vision-only document. Defines the problem and purpose of the system ("why"). Contains three requirements (Understanding Without Writing Code, Spec-Driven Context, Architecture Feedback Loop) that describe end-user outcomes, none of which are referenced by any `specs/prototype/` spec as something to implement. Per guidelines: "Specs under `specs/core/` describe the vision. Only create tasks from these if the prototype specs reference them." Prototype specs do not. **Permanently resolved.** |
| `specs/core/understanding-modes.spec.md` | **No tasks** | Defines Conformance Mode, Evaluation Mode, and Simulation Mode. All three are explicitly excluded from the prototype by `specs/prototype/prototype-scope.spec.md` Requirement: Not In Scope ("conformance mode is NOT implemented", "evaluation mode is NOT implemented", "simulation mode is NOT implemented"). Per guidelines: vision specs only generate tasks when prototype specs reference them. No prototype spec does. **Permanently resolved.** |
| `specs/index.spec.md` | **No tasks** | Table of contents. Contains 11 links and descriptive prose. Zero MUST/SHOULD/Scenario clauses. Zero implementation requirements. A table of contents cannot be "implemented." **Permanently resolved.** Condition to reopen: a commit that adds MUST/SHOULD/Scenario content to the file itself. |
| `specs/prototype/nfr.spec.md` | **No tasks** | Per project guidelines: "NFRs which are cross-cutting constraints, not standalone tasks." All five NFRs are acceptance criteria embedded in existing tasks, not independent work items. See NFR mapping below. **Permanently resolved.** |

## Zero tasks created

No task files were written. All four specs are closed with no-task decisions.

## NFR acceptance-criteria mapping

NFRs from `specs/prototype/nfr.spec.md` are constraints on existing implementation tasks,
not standalone deliverables:

| NFR | Constraint embedded in |
|---|---|
| Godot 4.6 engine; GDScript only; valid 4.6 API | All Godot application tasks (scene loading, rendering, camera, UX controls) |
| Python extractor; stdlib + ast/tree-sitter only | All Python extraction tasks (module discovery, dependency extraction, metrics, output) |
| JSON interface contract; extractor and app have no direct knowledge of each other | Task defining the schema (scene-graph-schema); task writing the output (extractor output); task loading the file (Godot scene loader) |
| Desktop platform; native Linux (Fedora); no browser/container/VM | Godot application tasks — build target is Linux desktop |
| Performance ≥ 30 fps at kartograph scale (~6 contexts, ~50 modules, ~100 files) | Rendering and camera tasks — verified against kartograph scene graph |
| Prototype disposability; iteration speed over code quality | Meta-constraint on all tasks; JSON scene graph format is the only artifact to preserve on pivot |

## Conditions to reopen any of these specs

- **system-purpose** — A `specs/prototype/` spec is added that explicitly implements
  one of: "Understanding Without Writing Code", "Spec-Driven Context", or "Architecture
  Feedback Loop".
- **understanding-modes** — A `specs/prototype/` spec is added that implements Conformance
  Mode, Evaluation Mode, or Simulation Mode (currently all excluded).
- **index.spec** — The file itself gains a MUST/SHOULD/Scenario clause (adding new links
  to the index does not qualify; those specs should be submitted as separate intake items).
- **nfr.spec** — A new NFR is added that cannot be expressed as an acceptance criterion on
  any existing task (e.g. it requires a dedicated implementation step).

## Re-queue log (2026-04-25 and later)

**ORCHESTRATOR: These specs are permanently resolved. "No tasks created" is a terminal
success state, not an error. The intake record for 2026-04-24 contains 500+ identical
rows documenting this same conclusion. Do not re-queue this batch.**

Root cause reminder: the orchestrator must track an `intake-resolved` set. If a spec is
in that set, skip it regardless of task count. Re-queueing when tasks=0 treats every
vision/NFR/index spec as perpetually unresolved and generates unbounded noise.

| Pass | Date | Outcome |
|---|---|---|
| 1 | 2026-04-25 | No tasks — all four specs permanently resolved (see analysis above). |
| 2 | 2026-04-25 | No tasks — identical outcome. Orchestrator re-queued. **Fix the orchestrator.** |
| 3 | 2026-04-25 | No tasks — identical outcome. All four specs permanently resolved. No new information. |
