# Intake Record — 2026-04-22

## Specs processed

| Spec | Decision | Reason |
|---|---|---|
| `specs/core/system-purpose.spec.md` | No tasks | Vision-only doc. No prototype spec requires implementing anything from it. |
| `specs/core/understanding-modes.spec.md` | No tasks | Defines Conformance, Evaluation, and Simulation modes, all three of which `specs/prototype/prototype-scope.spec.md` explicitly excludes from the prototype. |
| `specs/index.spec.md` | No tasks | Table of contents; contains no implementation requirements. |
| `specs/interaction/moldable-views.spec.md` | No tasks | Moldable views are explicitly excluded from the prototype scope. |
| `specs/prototype/nfr.spec.md` | No tasks | Per project guidelines, NFRs are cross-cutting constraints. Each NFR is captured below as an acceptance criterion on existing tasks. |
| `specs/visualization/data-flow.spec.md` | No tasks | Data flow visualization is explicitly excluded from the prototype scope. |

## NFR constraints on existing tasks

The following NFRs from `specs/prototype/nfr.spec.md` apply as acceptance criteria to existing tasks, not as new tasks.

- **Godot 4 engine** — Tasks 007–014 must target Godot 4.x; all scripts must be GDScript.
- **Python extractor** — Tasks 002–006 must use only the Python standard library and `tree-sitter` (or `ast`); no other third-party dependencies.
- **JSON interface contract** — The JSON file produced by task-006 is the only artifact consumed by task-007; neither side has direct knowledge of the other.
- **Desktop platform** — The Godot application (tasks 007–014) must run natively on Linux (Fedora) without browser, container, or VM dependencies.
- **Performance at kartograph scale** — Camera control and rendering tasks (008–014) must sustain ≥ 30 fps across kartograph's full structure (~6 bounded contexts, ~50 modules, ~100 files).
- **Prototype disposability** — Code quality is secondary to iteration speed; the JSON scene graph format (task-001) is the only artifact worth preserving if the prototype is discarded.
