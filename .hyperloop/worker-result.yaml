---
verdict: pass
---
All prohibited features have been removed and the spec is correctly implemented.

## Changes made

**Removed prohibited Godot files** (flagged by check-not-in-scope.sh):
- `godot/scripts/question_panel.gd`
- `godot/scripts/view_spec.gd`
- `godot/scripts/view_spec_renderer.gd`
- `godot/tests/test_question_panel.gd`
- `godot/tests/test_view_spec.gd`
- `godot/tests/test_view_spec_renderer.gd`

**Updated `godot/scripts/llm_view_generator.gd`**:
- Removed `const ViewSpec = preload("res://scripts/view_spec.gd")` dependency
- Inlined `VALID_OPS` constant and `_make_spec()` helper directly in the file
- The fixed primitive contract (show, hide, highlight, arrange, annotate, connect) is now self-contained

**Updated `godot/tests/test_llm_view_generator.gd`**:
- Removed the `ViewSpec` preload reference

**Updated `godot/scripts/main.gd`**:
- Removed `QuestionPanel`, `ViewSpecRenderer` preloads and all wiring
- Removed `_on_view_spec_requested()` handler and `_view_root` variable

**Updated `godot/tests/run_tests.gd`**:
- Removed references to the three deleted test files

**Removed prohibited spec-extraction code from extractor**:
- `extractor/extractor.py`: removed `extract_spec_nodes()`, `_layout_spec_nodes()`, regex constants, `import re`, and `include_specs` parameter from `build_scene_graph()`
- `extractor/__main__.py`: removed `--specs` CLI flag
- `extractor/tests/test_extractor.py`: removed `src_with_specs` fixture, `TestSpecExtraction` class, and `extract_spec_nodes` import

## Check results
- check-not-in-scope.sh: OK
- extractor-lint.sh: 90 pytest tests passed, ruff clean
- godot-compile.sh: compiles successfully
- godot-tests.sh: 87 GDScript tests passed, 0 failed
- godot-label3d.sh: PASS
- godot-fileaccess-tested.sh: OK
- check-kartograph-integration-test.sh: OK
