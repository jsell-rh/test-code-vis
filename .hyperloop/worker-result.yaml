---
verdict: pass
---
## Independent Reviewer Verdict — task-034
Branch: `hyperloop/task-034`
Spec: `specs/prototype/prototype-scope.spec.md`
Reviewer: independent code-review pass (2026-04-25)

## Check Suite

All 27 checks from `run-all-checks.sh` pass (synced from main before running).
GDScript: 100 passed, 0 failed.
Python: 97 passed, 0 failed.

**Verbatim run-all-checks.sh summary:**
```
=== Summary: 27 check(s) run ===
RESULT: ALL PASS
```

## Scope Check Output

```
OK: No prohibited (not-in-scope) features detected.
```

Independent audit of prohibited features (grep extractor/ godot/):
- conformance: (none)
- evaluation_mode: (none)
- simulation: (none)
- data_flow: (none)
- moldable/llm (build_prompt, parse_response, apply_spec, LlmView, etc.): (none)
- spec_extraction: (none)
- first_person: (none)

**Deleted-file provenance:** `understanding_analyzer.gd`, `understanding_overlay.gd`,
`test_understanding_modes.gd`, `test_nfr_scale.gd`, `test_system_purpose.gd`,
`test_understanding_overlay.gd` were pre-existing on `main` (from task prior that
implemented simulation mode). Commit `9ddabb8` on this branch correctly deleted them.
These are pre-existing artifacts, not introduced by task-034.

## Commit Trailers

Present on implementation commits (`9ddabb8`, `b9ff77f`, `44d8982`, etc.):
- `Spec-Ref: specs/prototype/prototype-scope.spec.md@12e8314c64416c10c5268a9d0f3ec54edb221c07`
- `Task-Ref: task-034`

## Entry-Point Stub Check

`main.gd._ready()` is NOT an empty stub. It contains full implementation:
FileAccess.open() → get_as_text() → JSON.parse() → SceneGraphLoader.load_from_dict()
→ build_from_graph(). All confirmed by reading main.gd lines 46-61.

## THEN→Test Mapping (Independent Verification)

| THEN-clause | Test | Predicate verified |
|---|---|---|
| kartograph structure visualized | `test_kartograph_integration_bounded_contexts` | Calls main() against ~/code/kartograph/src/api, asserts iam/graph/shared_kernel in output JSON ✓ |
| JSON scene graph produced | `test_main_writes_json_output` | CLI call, asserts output file exists and is valid JSON ✓ |
| Godot loads and renders scene | `test_volumes_created_for_each_node` | build_from_graph() with fixture, asserts Node3D anchors created ✓ |
| Bounded contexts visible as volumes | `test_far_distance_shows_only_bounded_contexts` | LOD FAR applied, asserts context visible, module hidden ✓ |
| Positions reflect coupling | `test_coupled_bcs_are_closer_than_uncoupled` | Algorithm-quality: fixture varies coupling; asserts dist(auth,shared_kernel) < dist(auth,billing) ✓ |
| Positions reflect coupling (combined) | `test_complexity_and_coupling_both_reflected` | Algorithm-quality: varies LOC AND coupling; asserts BOTH size and position relative outputs ✓ |
| Dependencies visible as connections | `test_dependency_expressed_as_visible_connection` | ImmediateMesh line rendered per edge ✓ |
| Internal layers visible on zoom | `test_medium_distance_shows_modules` | Module visible between FAR and NEAR thresholds ✓ |
| Internal dependencies shown | `test_near_distance_shows_internal_edges_as_fine_detail` | All edges visible below NEAR_THRESHOLD ✓ |
| LOD end-to-end | `test_lod_integration_far_hides_modules_in_built_scene` | Builds graph, applies FAR, asserts module hidden ✓ |
| Labeled geometric volume | `test_mesh_instances_exist_in_anchors` | MeshInstance3D children in anchors ✓ |
| Size reflects complexity | `test_large_module_has_bigger_mesh` | size=9 node has wider BoxMesh than size=3 ✓ |
| Size reflects complexity (proportional) | `test_mesh_sizes_proportional_to_metric` | BoxMesh.size.x ratio matches metric ratio ✓ |
| Containment by nesting | `test_bounded_context_is_translucent` | alpha<1.0, TRANSPARENCY_ALPHA on bounded_context material ✓ |
| Containment by nesting (parenting) | `test_containment_expressed_as_scene_tree_parenting` | mod_anchor.get_parent() == ctx_anchor ✓ |
| Label visible and readable | `test_labels_are_billboard_and_readable` | billboard==BILLBOARD_ENABLED, pixel_size>0.0, no_depth_test==true ✓ |
| Direction of dependency discernible | `test_direction_indicator_cone_created` | CylinderMesh with top_radius=0 exists (explicit arrowhead, not plain line) ✓ |
| Cone at target end | `test_direction_cone_near_target` | cone.position.distance_to(target) < 2.0 ✓ |
| Pan | `test_lmb_pan_moves_pivot` | LMB drag → pivot moves ✓ |
| Pan direction non-inverted (right) | `test_pan_drag_right_increases_pivot_x` | drag right (delta.x=+50) → pivot.x > initial; derivation comment verified against camera_controller.gd line 143 (`_pivot += right * delta.x * pan_amount`, no negation) ✓ |
| Pan direction non-inverted (left) | `test_pan_drag_left_decreases_pivot_x` | drag left (delta.x=-50) → pivot.x < initial ✓ |
| Zoom | `test_scroll_up_decreases_distance` + `test_scroll_down_increases_distance` | scroll up → distance decreases; scroll down → distance increases ✓ |
| Orbit | `test_orbit_horizontal_drag_changes_phi` + `test_orbit_vertical_drag_changes_theta` | RMB drag changes phi/theta ✓ |
| Smooth transition | `test_zoom_is_interpolated_not_instantaneous` | after scroll, _distance != _target_distance (interpolation in flight) ✓ |
| Not in scope (all 7 prohibited features) | `check-not-in-scope.sh` | Exits 0; independent grep confirms no prohibited code ✓ |

## Key Implementation Checks

- **Direction indicator**: CylinderMesh(top_radius=0) arrowhead at target end — satisfies "visually indicated" (not plain line alone). Confirmed in main.gd lines 248-267.
- **Pan sign derivation**: `_pivot += right * delta.x * pan_amount` (line 143, camera_controller.gd). No negation. Drag right → delta.x > 0 → pivot.x increases. Test asserts pivot.x > initial. Correct model (pivot follows drag = map-grab convention).
- **Algorithm-quality tests for "reflects" clauses**: Both coupling and complexity tests use fixtures that VARY the input property and assert RELATIVE output. These are NOT rendering-fidelity tests.
- **No wrong-predicate mappings found.**

## Process Notes

- `check-reflects-mapping-consistency.sh` SKIPS because the mapping table does not use the word "reflect(s)" as a column keyword. The reflects-THEN-clauses ARE covered (see table above) by algorithm-quality tests. This is a check-precision note, not a FAIL.
- `test_spatial_structure.gd` mentions "Scenario 1 – First-person exploration" in its docstring but includes an explicit note that first-person is NOT implemented. All test methods in that file exercise orbit/zoom/LOD — not first-person navigation.

## Verdict

**PASS.** All 27 checks pass. All spec THEN-clauses map to real, correctly-predicated tests. Prohibited features are absent. Implementation is non-trivial and substantive.
