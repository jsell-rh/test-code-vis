---
verdict: pass
---
## Spec Alignment Review — task-034
Branch: `hyperloop/task-034`
Spec: `specs/prototype/prototype-scope.spec.md`

## Requirement Coverage

### REQ: Target Codebase — COVERED
- Code: `extractor/extractor.py` → `build_scene_graph()`, `discover_bounded_contexts()`
  discovers Python packages as bounded contexts. `godot/data/scene_graph.json` was
  produced from `/home/jsell/code/kartograph/src/api` (confirmed via metadata.source_path)
  and contains 6 bounded contexts (graph, iam, infrastructure, management, query,
  shared_kernel) plus 36 module-level nodes.
- Tests:
  - `test_kartograph_integration_bounded_contexts` (`test_kartograph_integration.py`) —
    runs extractor CLI against real `~/code/kartograph/src/api`, asserts iam/graph/
    shared_kernel bounded contexts appear in the output JSON
  - `test_kartograph_extraction_produces_modules` — asserts at least one module-level
    node produced, confirming the actual codebase structure is reflected

### REQ: Two-Stage Pipeline — COVERED
- Code: `extractor/__main__.py::main()` parses CLI args, calls `build_scene_graph()`,
  writes JSON file. `godot/scripts/main.gd._ready()` reads JSON via `FileAccess.open()`
  + `get_as_text()`, parses with `JSON.new()`, delegates to `SceneGraphLoader.load_from_dict()`
  then `build_from_graph()`.
- Tests:
  - Python: `test_main_writes_json_output` (`test_cli.py`) — exercises full CLI,
    asserts output file exists and contains nodes/edges/metadata keys
  - GDScript: `test_volumes_created_for_each_node` + `test_mesh_instances_exist_in_anchors`
    (`test_scene_graph_loading.gd`) — exercises `build_from_graph()` with fixture data,
    asserts Node3D anchors and MeshInstance3D children are created

### REQ: Top-Down Architectural View — COVERED
- Code: `camera_controller.gd` starts with `_theta = 0.15` (~8.6° from straight above).
  `extractor.py._order_by_coupling()` + `compute_layout()` places coupled BCs adjacent
  in circular ring (closer together). `lod_manager.gd._apply_far()` hides module nodes
  at far distance. `main.gd._create_edge()` draws ImmediateMesh lines for every edge.
- Tests:
  - `test_initial_theta_is_near_top_down` (`test_camera_controls.gd`) — theta < PI/4
  - `test_far_distance_shows_only_bounded_contexts` (`test_spatial_structure.gd`)
  - `test_distinct_contexts_occupy_distinct_regions` (`test_spatial_structure.gd`)
  - `test_coupled_bcs_are_closer_than_uncoupled` (`test_extractor.py`) — 4-BC fixture,
    coupled pair auth↔shared_kernel asserted closer than uncoupled auth↔billing
  - `test_dependency_expressed_as_visible_connection` (`test_spatial_structure.gd`)

### REQ: Zoom to Detail — COVERED
- Code: `lod_manager.gd` with `FAR_THRESHOLD=80.0` / `NEAR_THRESHOLD=20.0`: `_apply_far()`
  hides modules + all edges; `_apply_medium()` shows modules + cross-context edges;
  `_apply_near()` shows everything including internal edges. Internal edges created by
  `main.gd._create_edge()`. Size encoding via `nd["size"]` (LOC-derived) applied to BoxMesh.
- Tests:
  - `test_medium_distance_shows_modules` — modules visible between FAR and NEAR thresholds
  - `test_near_distance_shows_internal_edges_as_fine_detail` — all edges visible at NEAR
  - `test_lod_integration_far_hides_modules_in_built_scene` — end-to-end LOD integration
  - `test_large_module_has_bigger_mesh` + `test_mesh_sizes_proportional_to_metric`
    (`test_size_encoding.gd`) — relative sizes proportional to the size metric

### REQ: Abstract Visual Language — COVERED
- Code: `main.gd._create_volume()` uses `BoxMesh` (geometric primitive) for all nodes.
  Bounded contexts: translucent slab (alpha=0.18, TRANSPARENCY_ALPHA, CULL_DISABLED).
  Modules: opaque box (alpha=1.0). Size from `nd["size"]`. Position from JSON layout data.
  Module anchors added as children of BC anchors for scene-tree nesting. No metaphors,
  buildings, terrain, or thematic decoration.
- Tests:
  - `test_bounded_context_is_translucent` + `test_module_is_opaque` + `test_bounded_context_larger_than_module`
    + `test_module_parented_inside_context` + `test_bounded_context_cull_disabled`
    (`test_containment_rendering.gd`)
  - `test_mesh_sizes_proportional_to_metric` (`test_size_encoding.gd`) — ratio correct
  - `test_complexity_and_coupling_both_reflected` (`test_extractor.py`) — both size
    (LOC-based complexity) and position (coupling-based) asserted in single integrated test

### REQ: Readable Labels — COVERED
- Code: `main.gd._create_volume()` creates `Label3D` with `label.text = nd["name"]`,
  `label.billboard = BaseMaterial3D.BILLBOARD_ENABLED`, `label.pixel_size = 0.012`,
  `label.no_depth_test = true` (visible through geometry at all zoom levels).
- Tests:
  - `test_labels_are_billboard_and_readable` (`test_scene_graph_loading.gd`) — asserts
    all three legibility properties on instantiated Label3D from fixture data

### REQ: Dependency Visualization — COVERED
- Code: `main.gd._create_edge()` builds `ImmediateMesh` (PRIMITIVE_LINES, 2 vertices)
  for the connecting line, and a `CylinderMesh` (top_radius=0 → cone) placed at the
  target end oriented along the edge direction as an arrowhead indicator.
  Color-coded: orange (1.0, 0.50, 0.10) for cross_context, grey (0.55, 0.55, 0.55)
  for internal.
- Tests:
  - `test_edge_line_mesh_created` — ImmediateMesh child exists after build_from_graph()
  - `test_direction_indicator_cone_created` — CylinderMesh with top_radius=0 present
  - `test_direction_cone_near_target` — cone within 2 units of target node position
  - `test_cross_context_cone_is_orange` — color assertion on cross-context edges
  (all in `test_dependency_rendering.gd`)

### REQ: Navigation — COVERED
- Code: `camera_controller.gd` — LMB→pan (`_pivot` shift in camera's local XZ plane),
  RMB→orbit (changes `_phi`/`_theta` spherical coordinates), scroll→smooth zoom
  (`_target_distance` set, `_distance` lerped toward it in `_process(delta)`).
- Tests:
  - Pan: `test_lmb_pan_moves_pivot`, `test_pan_drag_right_increases_pivot_x`,
    `test_pan_drag_left_decreases_pivot_x` (`test_ux_polish.gd`)
  - Zoom: `test_scroll_up_decreases_distance`, `test_scroll_down_increases_distance`,
    `test_zoom_clamped_at_minimum` (`test_camera_controls.gd`)
  - Rotate: `test_orbit_horizontal_drag_changes_phi`, `test_orbit_vertical_drag_changes_theta`
    (`test_camera_controls.gd`)
  - Smooth: `test_zoom_is_interpolated_not_instantaneous` (`test_ux_polish.gd`) —
    after scroll, `_distance != _target_distance` (animation in progress, not snapped)
  - Level transitions: `test_far_distance_shows_only_bounded_contexts` through
    `test_near_distance_shows_all_nodes` (`test_spatial_structure.gd`)

### REQ: Not In Scope — COVERED (all excluded features confirmed absent)
- Grepped `extractor/` and `godot/` production code for: `conformance`, `evaluation_mode`,
  `simulation`, `first_person`, `moldable`, `llm`, `build_prompt`, `parse_response`,
  `apply_spec`, `spec_extract` — zero matches in production code.
- `check-not-in-scope.sh` (part of 27-script automated check suite) exits 0.
- NOTE: `schema.py` declares `"spec"` as a valid NodeType and `lod_manager.gd` handles
  `ntype == "spec"` in visibility logic. This is latent forward-compatibility scaffolding
  only — the extractor produces no spec nodes, there is no conformance comparison logic,
  and no spec overlay UI exists. This does NOT constitute implementation of any excluded
  feature.

## Scope Check Output

OK: No prohibited (not-in-scope) features detected.

## Summary

All 9 SHALL/MUST requirements from `specs/prototype/prototype-scope.spec.md` are
COVERED. Every requirement has concrete implementation code and one or more behavioral
tests exercising the exact Given/When/Then conditions described in the scenario.
The 100 GDScript behavioral tests and 97 Python tests all pass. The 27-script automated
check suite passes entirely (27/27). Verdict: **PASS**.
