---
verdict: pass
---
## task-010: Godot Application (godot-application.spec.md) — re-run after PR feedback

### Problem addressed
The `check-not-in-scope.sh` check was failing because three categories of
prohibited features had been introduced in earlier cycles:

1. **Moldable views** — `godot/scripts/view_spec.gd`,
   `godot/scripts/view_spec_renderer.gd`, `godot/tests/test_view_spec.gd`,
   `godot/tests/test_view_spec_renderer.gd` and their references in `run_tests.gd`.
2. **Spec extraction** — `extract_spec_nodes()`, `_layout_spec_nodes()`,
   and the `include_specs` parameter in `extractor/extractor.py`.
3. **`--specs` CLI flag** — `extractor/__main__.py`.
4. **Spec-extraction tests** — `TestSpecExtraction` class and `src_with_specs`
   fixture in `extractor/tests/test_extractor.py`.

### Actions taken
- Deleted the four prohibited GDScript files.
- Removed moldable-view references from `godot/tests/run_tests.gd`.
- Stripped the entire "Spec extraction" section (≈100 lines) from
  `extractor/extractor.py` and simplified `build_scene_graph` signature.
- Removed `--specs` argument from `extractor/__main__.py`.
- Removed `extract_spec_nodes` import, `src_with_specs` fixture, and
  `TestSpecExtraction` class from `extractor/tests/test_extractor.py`.
- Removed now-unused `import re` from `extractor/extractor.py`.
- Ran `ruff format` to satisfy the lint check.

### Check results (all pass)
- `check-not-in-scope.sh` — **OK**
- `extractor-lint.sh` — **Passed** (90 tests, 0 failures)
- `godot-compile.sh` — **Passed**
- `godot-tests.sh` — **Passed**
- `godot-fileaccess-tested.sh` — **OK**
- `godot-label3d.sh` — **PASS**
- `check-kartograph-integration-test.sh` — **OK**

All spec requirements from `godot-application.spec.md` remain implemented
and tested — no spec coverage was lost by removing the out-of-scope code.
