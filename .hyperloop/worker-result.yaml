---
verdict: pass
---

## Summary

All prohibited (not-in-scope) artifacts have been removed and all checks now pass.

### Changes made

1. **Deleted** `godot/scripts/view_spec.gd` — moldable views are out of scope.
2. **Deleted** `godot/scripts/view_spec_renderer.gd` — same reason.
3. **Deleted** `godot/tests/test_view_spec.gd` — covered an out-of-scope feature.
4. **Deleted** `godot/tests/test_view_spec_renderer.gd` — same reason.
5. **Removed** `extract_spec_nodes`, `_layout_spec_nodes`, `include_specs` parameter,
   `_SPEC_HEADING_RE`, `_SPEC_LINK_RE`, and the `re` import from
   `extractor/extractor.py`.
6. **Removed** `--specs` CLI flag and `include_specs` call from
   `extractor/__main__.py`.
7. **Removed** `src_with_specs` fixture and `TestSpecExtraction` class from
   `extractor/tests/test_extractor.py`; removed `extract_spec_nodes` from its imports.
8. **Updated** `godot/tests/run_tests.gd` to remove the two preload calls that
   referenced the now-deleted `test_view_spec.gd` and `test_view_spec_renderer.gd`.
9. **Reformatted** `extractor/tests/test_extractor.py` via `ruff format` to satisfy
   the extractor-lint check.

## Scope Check Output

```
OK: No prohibited (not-in-scope) features detected.
```

## Check Results

- `check-not-in-scope.sh` → **OK** (exit 0)
- `extractor-lint.sh` → **OK** (exit 0): 90 tests pass, ruff clean
- `godot-compile.sh` → **OK** (exit 0): Godot 4.6.2 compiles the project
- `godot-fileaccess-tested.sh` → **OK** (exit 0)
- `godot-label3d.sh` → **OK** (exit 0)
- `godot-tests.sh` → **OK** (exit 0): all GDScript behavioral tests pass
