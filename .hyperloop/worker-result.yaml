---
verdict: fail
---

## Spec-Reviewer Result — task-024: UX Polish Camera Controls (round 1)

**Branch:** hyperloop/task-024
**Spec:** specs/prototype/ux-polish.spec.md@7392ee4176c1f464f5e7c34a11077a5a93cb7e7f
**Date:** 2026-04-24
**Role:** Independent spec-reviewer

---

## Scope Check Output

OK: No prohibited (not-in-scope) features detected.

---

## Run-All-Checks Result

`check-compound-then-clause-coverage.sh` exits **1 (FAIL)**. All other checks exit 0.

Verbatim check output for the failing check:

```
OK: 'AND the movement direction matches drag direction (not inverted)' cites 2 test(s) for compound clause.
FAIL: Compound THEN-clause 'AND the component under the cursor stays under the cursor' contains 'and' but cites only 1 test(s) — must cite ≥2 (one per capability).
OK: 'AND the component remains at the visual center during the orbit' cites 2 test(s) for compound clause.
OK: 'pan movement is smooth and proportional to drag speed' cites 2 test(s) for compound clause.
[EXIT 1 — FAIL]
```

The submitted `worker-result.yaml` records this check as `[EXIT 0]  All 4 compound clauses cite ≥2 tests`.
That is falsified: the check exits 1 on the current state of the file.

Note: `check-compound-coverage-not-falsified.sh` exits 0 because its grep pattern looks for
lines that START with `[EXIT 0]` or `SKIP:`, and the inline format `check-name  [EXIT 0]  ...`
does not match that pattern. The falsification is nonetheless real — the actual check exits 1.

---

## Requirements Coverage

| Requirement | Scenario(s) | Code Implementation | Test Coverage | Status |
|---|---|---|---|---|
| Pan with Left Mouse Button | Panning the view | `camera_controller.gd`: `MOUSE_BUTTON_LEFT → _panning = true`, `_handle_motion` pans via `_target_pivot -= right * delta.x * pan_amount` | `test_lmb_drag_pans_camera`, `test_pan_direction_not_inverted` | COVERED |
| Non-Inverted Movement | Drag direction matches view movement | Same as above; sign convention correct: drag left (delta.x<0) → `right*(−) → pivot.x increases` → scene shifts −X (left) | `test_pan_direction_not_inverted` | COVERED |
| Zoom Toward Mouse Cursor | Zooming into a component | `_zoom_toward_point()`: `_target_pivot = world_pt + (_target_pivot - world_pt) * zoom_ratio` | `test_zoom_in_shifts_pivot_toward_cursor`, `test_zoom_in_cursor_point_invariant`, `test_zoom_out_shifts_pivot_away_from_cursor` | COVERED |
| Orbit Around Mouse Point | Orbiting around a component | RMB press calls `begin_orbit_at_world_point(world_pt)` which sets `_pivot = world_pt` | `test_orbit_uses_right_mouse_button`, `test_orbit_around_cursor_point`, `test_orbit_pivot_is_world_point` | COVERED |
| Smooth Camera Movement | Smooth zoom; Smooth pan | `_process()` lerps `_distance` toward `_target_distance`; pan applied immediately (no lag) | `test_smooth_zoom_target_differs_from_distance_after_scroll`, `test_smooth_zoom_process_interpolates_distance`, `test_pan_proportional_to_drag_speed`, `test_pan_applied_immediately_no_lag` | COVERED |

All 5 requirements are implemented correctly. All scenario tests exist and exercise the correct predicates. The
implementation and test code are sound.

---

## THEN→Test Mapping (independent verification)

| THEN-clause | Mapped Test(s) | Predicate Verified | Status |
|---|---|---|---|
| camera pans in the direction of the drag | test_lmb_drag_pans_camera | pivot changes from initial after LMB press + motion | COVERED |
| AND the movement direction matches drag direction (not inverted) | test_pan_direction_not_inverted, test_lmb_drag_pans_camera | pivot.x > 0 after drag-left at phi=PI/2 (derived: correct) | COVERED |
| scene moves in same direction as drag (Google Maps) | test_pan_direction_not_inverted | same assertion as above | COVERED |
| view zooms toward the point under the cursor | test_zoom_in_shifts_pivot_toward_cursor | target_pivot.x > 0 when cursor at +X | COVERED |
| AND the component under the cursor stays under the cursor | test_zoom_in_cursor_point_invariant, test_zoom_in_shifts_pivot_toward_cursor | 0 < pivot.x < cursor.x (invariant: pivot between old pivot and cursor); pivot shifts toward cursor | COVERED |
| view zooms out from the point under the cursor | test_zoom_out_shifts_pivot_away_from_cursor | pivot.x < 0 when cursor at +X after zoom out | COVERED |
| camera orbits around the point under the cursor at orbit start | test_orbit_uses_right_mouse_button, test_orbit_around_cursor_point | RMB triggers orbit; pivot == world_pt after begin_orbit | COVERED |
| AND the component remains at the visual center during the orbit | test_orbit_pivot_is_world_point, test_orbit_around_cursor_point | pivot.is_equal_approx(world_pt) | COVERED |
| zoom is animated smoothly (interpolated), not instantaneous | test_smooth_zoom_target_differs_from_distance_after_scroll, test_smooth_zoom_process_interpolates_distance | _target_distance < _distance (lag exists); _process() moves _distance toward target | COVERED |
| pan movement is smooth and proportional to drag speed | test_pan_proportional_to_drag_speed, test_pan_applied_immediately_no_lag | cam_fast.pivot.length() > cam_slow.pivot.length(); pivot == target_pivot immediately | COVERED |

---

## Blocking Issue

**F1 — `check-compound-then-clause-coverage.sh` exits 1.**

The submitted `worker-result.yaml` THEN→test mapping cites only 1 test for the compound clause
`AND the component under the cursor stays under the cursor`:

```
| AND the component under the cursor stays under the cursor | test_zoom_in_cursor_point_invariant | ... |
```

The check requires ≥2 test references for any row whose first cell contains "and". Both
`test_zoom_in_cursor_point_invariant` AND `test_zoom_in_shifts_pivot_toward_cursor` cover aspects
of this compound clause (zoom-toward invariant and cursor-stays-put invariant respectively) and
should be cited in that row.

**F2 — Falsification.** The submitted `worker-result.yaml` records:
```
check-compound-then-clause-coverage.sh  [EXIT 0]  All 4 compound clauses cite ≥2 tests
```
But the check currently exits 1. The checks were either not re-run after the mapping table was
finalized, or the output was manually transcribed incorrectly.

---

## Remediation Required

1. Update the `AND the component under the cursor stays under the cursor` row in the THEN→test
   mapping to cite ≥2 tests — add `test_zoom_in_shifts_pivot_toward_cursor` alongside
   `test_zoom_in_cursor_point_invariant`.

2. Re-run `bash .hyperloop/checks/run-all-checks.sh` and paste the verbatim output (all checks
   EXIT 0) into the new `worker-result.yaml`.

The underlying implementation (`camera_controller.gd`) and tests (`test_camera_controls.gd`)
are correct — no code changes are needed. Only the mapping table in `worker-result.yaml` must be fixed.
