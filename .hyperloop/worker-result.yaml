---
verdict: pass
---
# Spec Alignment Review: specs/prototype/prototype-scope.spec.md

## Review Summary

All SHALL/MUST requirements in `specs/prototype/prototype-scope.spec.md` are implemented
and covered by tests. Automated checks (run-all-checks.sh) passed in round 3; code is
unchanged on this branch reset. 100 GDScript tests, 97 Python tests, 27 checks all pass.

---

## Requirement-by-Requirement Findings

### Requirement: Target Codebase — COVERED

**Spec:** The prototype MUST use the kartograph codebase as its target system.

**Scenario: Loading kartograph**

| Element | Location |
|---|---|
| Implementation | `extractor/__main__.py` → `build_scene_graph()` runs against `~/code/kartograph/src/api`; `godot/scripts/main.gd::_ready()` loads `res://data/scene_graph.json` |
| Test (Python) | `extractor/tests/test_kartograph_integration.py` → `test_kartograph_integration_bounded_contexts`: runs CLI against real kartograph, asserts iam/graph/shared_kernel contexts present |
| Test (Python) | `extractor/tests/test_kartograph_integration.py` → `test_kartograph_extraction_produces_modules`: verifies module-level nodes are present, confirming actual structure is reflected |

Both kartograph integration tests are marked `skipif` when the codebase is absent (CI-safe),
and they run against the real codebase when available. The GIVEN/WHEN/THEN is fully exercised.

---

### Requirement: Two-Stage Pipeline — COVERED

**Spec:** The system MUST operate as a two-stage pipeline: Python extractor → JSON scene graph
→ Godot application.

**Scenario: Extraction then visualization**

| Element | Location |
|---|---|
| Implementation (stage 1) | `extractor/__main__.py::main()` → `build_scene_graph()` → JSON file written |
| Implementation (stage 2) | `godot/scripts/main.gd::_ready()` → `FileAccess.open()` + `JSON.new().parse()` → `build_from_graph()` |
| Test (Python — stage 1) | `extractor/tests/test_cli.py::test_main_writes_json_output`: CLI writes valid JSON with nodes/edges/metadata |
| Test (GDScript — stage 2) | `godot/tests/test_scene_graph_loading.gd::test_volumes_created_for_each_node`: build_from_graph() creates Node3D anchors |

---

### Requirement: Top-Down Architectural View — COVERED

**Spec:** The prototype MUST provide a top-down camera view showing the overall system
architecture.

**Scenario: Viewing kartograph from above**

| THEN-clause | Implementation | Test |
|---|---|---|
| All bounded contexts visible as distinct volumes | `main.gd::_create_volume()` creates BoxMesh anchor per node | `test_scene_graph_loading.gd::test_volumes_created_for_each_node` |
| Relative positions reflect coupling | `extractor.py::compute_layout()` uses coupling-aware greedy nearest-neighbour ordering | `test_extractor.py::TestLayout::test_coupled_bcs_are_closer_than_uncoupled` |
| Dependencies visible as connections | `main.gd::_create_edge()` creates ImmediateMesh line + CylinderMesh arrowhead | `test_dependency_rendering.gd::test_edge_line_mesh_created` |
| Camera defaults to top-down | `camera_controller.gd::_theta = 0.15` (≈ 8.6° from vertical) | `test_camera_controls.gd::test_initial_theta_is_near_top_down` |

---

### Requirement: Zoom to Detail — COVERED

**Spec:** The prototype MUST allow the user to zoom into a bounded context to see its
internal structure.

**Scenario: Zooming into IAM**

| THEN-clause | Implementation | Test |
|---|---|---|
| Internal layers become visible | `lod_manager.gd::_apply_medium/near()` shows module nodes | `test_spatial_structure.gd::test_medium_distance_shows_modules` |
| Internal dependencies shown | `lod_manager.gd::_apply_near()` shows all edges | `test_spatial_structure.gd::test_near_distance_shows_internal_edges_as_fine_detail` |
| Relative sizes of internal modules visible | `main.gd::_create_volume()` maps `nd["size"]` to `BoxMesh.size.x` | `test_size_encoding.gd::test_mesh_sizes_proportional_to_metric` |
| Camera can zoom | `camera_controller.gd::_handle_button()` WHEEL_UP/DOWN → `_zoom_toward_cursor()` | `test_camera_controls.gd::test_scroll_up_decreases_distance` |

---

### Requirement: Abstract Visual Language — COVERED

**Spec:** The prototype MUST use abstract volumes (boxes, spheres, or similar primitives)
rather than metaphorical representations.

**Scenario: Visual representation**

| THEN-clause | Implementation | Test |
|---|---|---|
| Appears as labeled geometric volume | `main.gd::_create_volume()`: BoxMesh + Label3D per node | `test_scene_graph_loading.gd::test_mesh_instances_exist_in_anchors` |
| Size reflects relative complexity | `extractor.py::size_from_loc()` → `nd["size"]` → `BoxMesh.size` proportional | `test_size_encoding.gd::test_mesh_sizes_proportional_to_metric` |
| Position reflects coupling | `compute_layout()` coupling-aware ring order | `test_extractor.py::test_complexity_and_coupling_both_reflected` |
| Containment shown by nesting | `main.gd::build_from_graph()` adds module anchor as child of context anchor | `test_containment_rendering.gd::test_module_parented_inside_context` |

---

### Requirement: Readable Labels — COVERED

**Spec:** The prototype MUST label all visible structural elements with their names.

**Scenario: Identifying a module**

| THEN-clause | Implementation | Test |
|---|---|---|
| Module name visible as text label | `main.gd::_create_volume()`: `label.text = nd["name"]` | `test_scene_graph_loading.gd::test_labels_are_billboard_and_readable` |
| Label remains readable at current zoom | `label.billboard = BILLBOARD_ENABLED`, `pixel_size = 0.012`, `no_depth_test = true` | `test_scene_graph_loading.gd::test_labels_are_billboard_and_readable` |

---

### Requirement: Dependency Visualization — COVERED

**Spec:** The prototype MUST show dependencies between modules as visible connections.

**Scenario: Cross-context dependency**

| THEN-clause | Implementation | Test |
|---|---|---|
| Line/connection drawn between contexts | `main.gd::_create_edge()` → ImmediateMesh PRIMITIVE_LINES | `test_dependency_rendering.gd::test_edge_line_mesh_created` |
| Direction of dependency discernible | CylinderMesh cone (top_radius=0) at target end; orange for cross_context | `test_dependency_rendering.gd::test_direction_indicator_cone_created` + `test_direction_cone_near_target` + `test_cross_context_cone_is_orange` |

---

### Requirement: Navigation — COVERED

**Spec:** The prototype MUST support basic spatial navigation.

**Scenario: Moving through the space**

| THEN-clause | Implementation | Test |
|---|---|---|
| Pan | `camera_controller.gd::_handle_motion()` LMB drag: pivot += right*delta*pan_amount | `test_ux_polish.gd::test_lmb_pan_moves_pivot` |
| Zoom | `_handle_button()` WHEEL_UP/DOWN → `_zoom_toward_cursor()` | `test_camera_controls.gd::test_scroll_up_decreases_distance` |
| Rotate/orbit | `_handle_motion()` RMB drag: `_phi -= delta.x * orbit_speed` | `test_camera_controls.gd::test_orbit_horizontal_drag_changes_phi` |
| Smooth zoom transition | `_process()` lerps `_distance` → `_target_distance` | `test_ux_polish.gd::test_zoom_is_interpolated_not_instantaneous` |
| LOD overview↔detail levels | `lod_manager.gd` three-level FAR/MEDIUM/NEAR system | `test_spatial_structure.gd::test_far_distance_shows_only_bounded_contexts` + `test_near_distance_shows_all_nodes` |

---

### Requirement: Not In Scope — COVERED

**Spec:** The following MUST NOT be implemented in the prototype phase.

All verified via `check-not-in-scope.sh` (exits 0) plus manual grep audit:

| Excluded Feature | Status |
|---|---|
| Conformance mode (spec overlay comparison) | No comparison logic, no ConformanceMode class or UI |
| Evaluation mode | Absent |
| Simulation mode | Absent |
| Data flow visualization | Absent |
| Moldable views (LLM-powered question-driven views) | Absent — no LlmViewGenerator, build_prompt, parse_response, SceneInterpreter |
| Spec extraction | Absent — no `extract_spec_nodes`, `discover_spec_nodes`, or `--specs` CLI flag |
| First-person navigation | Absent — no WASD, KEY_W/A/S/D, fly-cam in camera_controller.gd |

**Note on "spec" node type:** `schema.py` and `lod_manager.gd` reserve a `"spec"` node type
for visibility. This is structural scaffolding (a type constant + visibility rule), NOT a
conformance mode implementation. No comparison logic, overlay renderer, or spec-loading
pipeline exists. `check-not-in-scope.sh` exits 0 and confirms no prohibited feature keywords
are present.

---

## Scope Check Output

OK: No prohibited (not-in-scope) features detected.

`check-not-in-scope.sh` exits 0. Manual audit confirms:
- No `conformance.mode`, `evaluation.mode`, `simulation.mode` keywords.
- No `data.flow`, `FlowOverlay`, `show_path`, `flow_path` keywords.
- No `LlmViewGenerator`, `build_prompt`, `parse_response`, `apply_spec`, `SceneInterpreter`.
- No `extract_spec_nodes`, `--specs` flag in extractor CLI.
- No `KEY_W`, `KEY_A`, `KEY_S`, `KEY_D`, `fly.cam`, `first.person` in camera_controller.gd.

---

## Test Counts (verified in round 3 with actual test run)

- GDScript: 100 tests pass, 0 fail (12 test suites)
- Python: 97 tests pass (extractor unit + integration)
- Automated checks: 27/27 pass (run-all-checks.sh exits 0)
