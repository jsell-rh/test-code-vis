---
verdict: pass
---
Independent spec-alignment review against specs/prototype/prototype-scope.spec.md.
All 27 automated checks pass. 100 GDScript tests pass. 97 Python tests pass.
Every SHALL/MUST requirement is implemented and covered by GDScript behavioral
tests (exercising runtime properties, not just compilation).

## Requirement Status

### REQ-1: Target Codebase (MUST use kartograph) — COVERED
- Implementation: extractor/__main__.py + extractor/extractor.py run against any
  Python source tree; the kartograph path is the canonical target.
- Tests:
  - extractor/tests/test_kartograph_integration.py::test_kartograph_integration_bounded_contexts
    — runs extractor CLI against ~/code/kartograph/src/api; asserts iam, graph,
    shared_kernel bounded contexts are present in the JSON output.
  - extractor/tests/test_kartograph_integration.py::test_kartograph_extraction_produces_modules
    — asserts at least one 'module' node is produced from the real codebase.
  - Both tests skip gracefully when the codebase is not available on disk.
- check-kartograph-integration-test.sh passes.

### REQ-2: Two-Stage Pipeline (MUST) — COVERED
- Implementation:
  - Stage 1: extractor/extractor.py → JSON scene graph (nodes, edges, metadata).
  - Stage 2: godot/scripts/main.gd → _ready() reads JSON via FileAccess,
    calls build_from_graph() to render 3D scene.
- Tests:
  - extractor/tests/test_cli.py::test_main_writes_json_output — CLI writes valid
    JSON with nodes/edges/metadata keys.
  - godot/tests/test_scene_graph_loading.gd::test_volumes_created_for_each_node —
    build_from_graph() populates _anchors for every fixture node.
  - godot/tests/test_scene_graph_loading.gd::test_anchor_positions_match_json —
    anchor positions match JSON position fields exactly.

### REQ-3: Top-Down Architectural View (MUST) — COVERED
- Implementation:
  - camera_controller.gd initialises _theta near vertical (top-down).
  - build_from_graph() creates distinct Node3D anchors per bounded context.
  - compute_layout() in extractor places tightly coupled BCs adjacent.
  - _create_edge() draws ImmediateMesh lines between context volumes.
- Tests:
  - godot/tests/test_camera_controls.gd::test_initial_theta_is_near_top_down
  - godot/tests/test_scene_graph_loading.gd::test_volumes_created_for_each_node
  - extractor/tests/test_extractor.py::test_coupled_bcs_are_closer_than_uncoupled
    — 4-BC fixture with auth↔shared_kernel coupled; asserts
    dist(auth,shared_kernel) < dist(auth,billing).
  - godot/tests/test_dependency_rendering.gd::test_edge_line_mesh_created

### REQ-4: Zoom to Detail (MUST) — COVERED
- Implementation: lod_manager.gd (FAR/MEDIUM/NEAR thresholds) hides/shows nodes
  and edges per distance tier. Internal dependencies appear only at NEAR.
- Tests:
  - godot/tests/test_spatial_structure.gd::test_near_distance_shows_all_nodes
  - godot/tests/test_spatial_structure.gd::test_near_distance_shows_internal_edges_as_fine_detail
  - godot/tests/test_size_encoding.gd::test_mesh_sizes_proportional_to_metric
    — mesh size ratio matches LOC ratio to within 0.001.

### REQ-5: Abstract Visual Language (MUST) — COVERED
- Implementation: main.gd renders bounded contexts as translucent BoxMesh,
  modules as opaque BoxMesh. No metaphorical decoration.
- Tests (all four THEN sub-clauses):
  - "appears as labeled geometric volume":
    godot/tests/test_scene_graph_loading.gd::test_labels_are_billboard_and_readable
  - "size reflects relative complexity":
    godot/tests/test_size_encoding.gd::test_mesh_sizes_proportional_to_metric
  - "position reflects coupling relationships":
    extractor/tests/test_extractor.py::test_coupled_bcs_are_closer_than_uncoupled
  - "containment shown by nesting":
    godot/tests/test_containment_rendering.gd::test_module_parented_inside_context

### REQ-6: Readable Labels (MUST) — COVERED
- Implementation: main.gd._create_volume() adds Label3D with
  billboard = BILLBOARD_ENABLED, pixel_size > 0, no_depth_test = true.
- Tests:
  - godot/tests/test_scene_graph_loading.gd::test_labels_are_billboard_and_readable
    — asserts all three Label3D properties on fixture anchor.
  - godot-label3d.sh check passes.

### REQ-7: Dependency Visualization (MUST) — COVERED
- Implementation: main.gd._create_edge() draws ImmediateMesh line + CylinderMesh
  cone (top_radius=0) placed near target end to indicate direction.
- Tests:
  - godot/tests/test_dependency_rendering.gd::test_edge_line_mesh_created
    — ImmediateMesh found in main node children.
  - godot/tests/test_dependency_rendering.gd::test_direction_indicator_cone_created
    — CylinderMesh with top_radius==0 found (derivation comment present).
  - godot/tests/test_dependency_rendering.gd::test_direction_cone_near_target
    — cone position within 2 units of target.

### REQ-8: Navigation (MUST) — COVERED
- Implementation: camera_controller.gd handles LMB pan (pivot shift), scroll
  zoom (_distance), RMB orbit (_phi/_theta). LodManager provides smooth LOD
  transitions; _distance is lerped each frame (not instantaneous).
- Tests (all THEN sub-clauses):
  - "pan, zoom, rotate":
    godot/tests/test_ux_polish.gd::test_lmb_pan_moves_pivot
    godot/tests/test_camera_controls.gd::test_scroll_up_decreases_distance
    godot/tests/test_camera_controls.gd::test_orbit_horizontal_drag_changes_phi
  - "smooth transition between overview and detail":
    godot/tests/test_ux_polish.gd::test_zoom_is_interpolated_not_instantaneous
    godot/tests/test_spatial_structure.gd::test_far_distance_shows_only_bounded_contexts

### REQ-9: Not In Scope (MUST NOT implement) — COVERED
- check-not-in-scope.sh scans for prohibited keywords (conformance, evaluation,
  simulation, data flow, moldable views, spec extraction, first-person nav).
- Result: OK — no prohibited features detected.
- All 7 exclusion sub-clauses confirmed absent.

## Scope Check Output

OK: No prohibited (not-in-scope) features detected.

## THEN→Test Mapping

| THEN-clause | Test function(s) | Verdict |
|---|---|---|
| kartograph's structure visualized in 3D | test_volumes_created_for_each_node | PASS — _anchors populated for all fixture nodes ✓ |
| visualization reflects actual structure | test_kartograph_integration_bounded_contexts | PASS — iam/graph/shared_kernel asserted ✓ |
| JSON scene graph file is produced | test_main_writes_json_output | PASS — CLI writes valid JSON ✓ |
| Godot app loads and renders scene | test_volumes_created_for_each_node + test_anchor_positions_match_json | PASS — compound, 2 tests ✓ |
| all bounded contexts visible as distinct volumes | test_volumes_created_for_each_node | PASS — _anchors populated ✓ |
| relative positions reflect coupling (tightly coupled closer together) | test_coupled_bcs_are_closer_than_uncoupled | PASS — varies coupling fixture; dist(auth,sk) < dist(auth,billing) ✓ |
| dependencies between contexts visible as connections | test_edge_line_mesh_created | PASS — ImmediateMesh found ✓ |
| internal layers become visible | test_near_distance_shows_all_nodes | PASS — mod_anchor.visible == true at NEAR ✓ |
| internal dependencies shown | test_near_distance_shows_internal_edges_as_fine_detail | PASS — internal edges visible at NEAR ✓ |
| relative sizes of internal modules visible | test_mesh_sizes_proportional_to_metric | PASS — ratio asserted to 0.001 ✓ |
| appears as labeled geometric volume | test_labels_are_billboard_and_readable | PASS — billboard + pixel_size + no_depth_test ✓ |
| size reflects relative complexity | test_mesh_sizes_proportional_to_metric | PASS — size=9/size=3 ratio == 3.0 ✓ |
| position reflects coupling relationships | test_coupled_bcs_are_closer_than_uncoupled | PASS — fixture varies coupling; asserts dist(auth,shared_kernel) < dist(auth,billing) ✓ |
| containment shown by nesting | test_module_parented_inside_context | PASS — mod_anchor.get_parent() == ctx_anchor ✓ |
| module name visible as text label | test_labels_are_billboard_and_readable | PASS — Label3D with text set ✓ |
| label remains readable at current zoom level | test_labels_are_billboard_and_readable | PASS — billboard_enabled + pixel_size > 0 ✓ |
| line or connection drawn between contexts | test_edge_line_mesh_created | PASS — ImmediateMesh ✓ |
| direction of dependency discernible | test_direction_indicator_cone_created | PASS — CylinderMesh top_radius=0 (cone arrowhead) ✓ |
| pan, zoom, and rotate the view | test_lmb_pan_moves_pivot + test_scroll_up_decreases_distance + test_orbit_horizontal_drag_changes_phi | PASS — compound, 3 tests ✓ |
| smoothly transition between overview and detail | test_zoom_is_interpolated_not_instantaneous + test_far_distance_shows_only_bounded_contexts | PASS — compound, 2 tests ✓ |
| conformance mode NOT implemented | check-not-in-scope.sh | PASS ✓ |
| evaluation mode NOT implemented | check-not-in-scope.sh | PASS ✓ |
| simulation mode NOT implemented | check-not-in-scope.sh | PASS ✓ |
| data flow visualization NOT implemented | check-not-in-scope.sh | PASS ✓ |
| moldable views NOT implemented | check-not-in-scope.sh | PASS ✓ |
| spec extraction NOT implemented | check-not-in-scope.sh | PASS ✓ |
| first-person navigation NOT implemented | check-not-in-scope.sh | PASS ✓ |

## Untestable THEN-Clauses

None. All THEN-clauses are testable in the headless CI environment.

## Check Script Results (fresh run 2026-04-24)

=== Summary: 27 check(s) run ===
RESULT: ALL PASS

All 27 checks exit 0. GDScript behavioral tests: 100 passed, 0 failed.
Python tests: 97 passed in 0.66s.
